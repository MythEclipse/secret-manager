# Secret Manager Skill

Access and manage secure secrets via MCP.

## Capabilities
- Add secrets (encrypted)
- Retrieve secrets by name
- List available secret names

## Configuration
Requires `SM_PASSWORD` environment variable.

## Usage
Triggered automatically when the user asks for secrets or credentials managed by this tool.
